<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body class="flex flex-col min-h-screen">
    <%- include('./partials/navbar.ejs') %>
    <main
      class="flex flex-col items-center justify-center w-full h-full flex-1 bg-gray-50"
    >
      <h1
        class="w-full max-w-md text-2xl font-bold my-4 p-6 m-4 rounded-lg shadow-lg shadow-gray-500/50 bg-white"
      >
        Title: <%= post.title %>
      </h1>
      <div class="flex gap-2">
        <p class="font-bold">By: <%= post.username %></p>
        <p class="font-gray-500"><%= post.email %></p>
        <p><%= post.first_name %> <%= post.last_name %></p>
      </div>
      <p>Date Posted: <%= post.formatted_date %></p>

      <div
        class="p-6 m-4 rounded-lg shadow-lg shadow-gray-500/50 bg-white max-w-md w-full"
      >
        <p class="my-4"><%= post.content %></p>
      </div>
      <% if (post.user_id === user_id) { %>
      <form
        action="/post/<%= post.post_id %>?_method=DELETE"
        method="POST"
        onsubmit="return confirm('Are you sure you want to delete this post?')"
      >
        <button
          class="bg-red-500 text-white font-semibold py-2 px-4 rounded-md hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-red-500 my-2"
          type="submit"
        >
          Delete
        </button>
      </form>
      <% } %>
    </main>
  </body>
</html>
